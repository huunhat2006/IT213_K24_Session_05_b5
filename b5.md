# BÀI 5: SÁNG TẠO NÂNG CAO - THIẾT KẾ TRỢ LÝ ẢO TRA CỨU CRM & ÁP DỤNG VOUCHER TỰ ĐỘNG

---

## 1. MÔ TẢ BỐI CẢNH & PHÂN TÍCH GIẢI PHÁP CHỊU LỖI (FAULT-TOLERANCE DESIGN)

### 1.1. Bối cảnh nghiệp vụ
Hệ thống R-Hotels triển khai module **CRM Support Agent** giúp tự động hóa quy trình chăm sóc khách hàng và tối ưu hóa ưu đãi:
1. Khách hàng gửi yêu cầu tự nhiên: *"Áp dụng giúp tôi mã giảm giá tốt nhất của tôi vào đơn đặt phòng mã HD999 nhé."*
2. Trợ lý AI đọc bộ nhớ `ChatMemory` để kiểm tra danh tính khách hàng (Mã `customerId` hoặc số điện thoại). Nếu chưa có, AI chủ động yêu cầu khách hàng định danh.
3. Khi đã có `customerId` (ví dụ: `KH888`), AI tự động gọi **Tool 1 (`getCustomerVouchers`)** để truy xuất danh sách mã giảm giá còn hiệu lực từ hệ thống CRM (ví dụ: `VIP20` giảm 20% max 500k, `WELCOME10` giảm 10% max 200k).
4. AI phân tích các lựa chọn và tự động quyết định chọn voucher có giá trị chiết khấu cao nhất (`VIP20`).
5. AI tự động gọi tiếp **Tool 2 (`applyVoucherToInvoice`)** để chèn mã `VIP20` vào hóa đơn `HD999` trong database backend.
6. AI tổng hợp kết quả và trả lời câu thoại tự nhiên thông báo số tiền đã giảm và số tiền còn lại phải thanh toán cho khách hàng.

---

### 1.2. Phân tích giải pháp thiết kế chịu lỗi nghiệp vụ (Business Fault Tolerance)
Xử lý voucher và hóa đơn là tác vụ nhạy cảm dễ phát sinh lỗi nghiệp vụ. Để đảm bảo hệ thống **không bao giờ sập (Zero Crash Policy)**, mã nguồn Java áp dụng 3 nguyên tắc lập trình phòng thủ:

* **Không bao giờ quăng Exception từ phương thức `@Tool`:** Việc ném `RuntimeException` trong Tool sẽ làm đứt vòng lặp ReAct của Spring AI và gây sập HTTP 500. Thay vào đó, tất cả lỗi nghiệp vụ (như *Hóa đơn HD888 đã thanh toán trước đó*, *Mã khách hàng không tồn tại*, *Voucher quá hạn*) đều được đóng gói thành DTO Response chứa cờ `isSuccess = false` kèm thông điệp giải thích trong `message`.
* **Cơ chế AI Self-Correction (Tự đính chính hội thoại):** Khi nhận được JSON payload phản hồi `isSuccess = false`, Spring AI truyền dữ liệu lỗi này lại cho LLM. LLM dựa vào `message` lỗi để trả lời lịch sự cho khách hàng (ví dụ: *"Dạ đơn đặt phòng HD888 của anh/chị đã được thanh toán trước đó rồi nên hệ thống không thể áp dụng thêm voucher nữa ạ"*).
* **Validation đa tầng tại DTO và Service:** Kiểm tra `null`, `blank`, trim khoảng trắng và chuẩn hóa chuỗi hoa/thường trước khi thực hiện logic truy vấn database.

---

## 2. SƠ ĐỒ LUỒNG XỬ LÝ DỮ LIỆU (ASCII FLOW DIAGRAM)

```
                       ┌─────────────────────────┐
                       │   Khách hàng gửi chat   │
                       └────────────┬────────────┘
                                    │
                                    ▼
                       ┌─────────────────────────┐
                       │ REST Controller / Prompt│
                       └────────────┬────────────┘
                                    │
                                    ▼
                     ┌─────────────────────────────┐
                     │ Kiểm tra ChatMemory (Session)│
                     └──────────────┬──────────────┘
                                    │
                    Có thông tin Customer ID (KH888)?
                   ┌────────────────┴────────────────┐
                  KHÔNG                              CÓ
                   │                                 │
                   ▼                                 ▼
    ┌──────────────────────────────┐  ┌──────────────────────────────┐
    │ Yêu cầu khách hàng cung cấp  │  │ AI gọi Tool 1:               │
    │ mã Customer ID / Số ĐT       │  │ getCustomerVouchers(KH888)   │
    └──────────────────────────────┘  └──────────────┬───────────────┘
                                                     │
                                                     ▼
                                      ┌──────────────────────────────┐
                                      │ Trả về danh sách Vouchers:   │
                                      │ - VIP20 (Giảm 20%, max 500k) │
                                      │ - WELCOME10 (Giảm 10%)       │
                                      └──────────────┬───────────────┘
                                                     │
                                                     ▼
                                      ┌──────────────────────────────┐
                                      │ AI phân tích chọn Voucher    │
                                      │ tốt nhất -> Mã VIP20         │
                                      └──────────────┬───────────────┘
                                                     │
                                                     ▼
                                      ┌──────────────────────────────┐
                                      │ AI gọi Tool 2:               │
                                      │ applyVoucherToInvoice(HD999, │
                                      │                       VIP20) │
                                      └──────────────┬───────────────┘
                                                     │
                                                     ▼
                                      ┌──────────────────────────────┐
                                      │ Cập nhật Database & Trả về:  │
                                      │ - isSuccess: true            │
                                      │ - Đã giảm: 500.000 VNĐ       │
                                      │ - Giá còn lại: 3.500.000 VNĐ │
                                      └──────────────┬───────────────┘
                                                     │
                                                     ▼
                                      ┌──────────────────────────────┐
                                      │ AI tổng hợp câu trả lời tự   │
                                      │ nhiên gửi về cho Khách hàng  │
                                      └──────────────────────────────┘
```

---

## 3. MÃ NGUỒN JAVA TRIỂN KHAI HOÀN CHỈNH

### 3.1. Các DTOs (`b5/dto/`)

#### `VoucherDTO.java`
```java
package com.rhotels.crm.dto;

public record VoucherDTO(
    String code,
    String description,
    double discountPercentage,
    double maxDiscountAmount,
    boolean isExpired
) {}
```

#### `CustomerVouchersRequest.java`
```java
package com.rhotels.crm.dto;

import com.fasterxml.jackson.annotation.JsonPropertyDescription;

public record CustomerVouchersRequest(
    @JsonPropertyDescription("Mã định danh duy nhất của khách hàng trong hệ thống CRM (ví dụ: KH888)")
    String customerId
) {}
```

#### `CustomerVouchersResponse.java`
```java
package com.rhotels.crm.dto;

import java.util.List;

public record CustomerVouchersResponse(
    boolean isSuccess,
    String customerId,
    List<VoucherDTO> vouchers,
    String message
) {
    public static CustomerVouchersResponse error(String message) {
        return new CustomerVouchersResponse(false, null, List.of(), message);
    }

    public static CustomerVouchersResponse success(String customerId, List<VoucherDTO> vouchers, String message) {
        return new CustomerVouchersResponse(true, customerId, vouchers, message);
    }
}
```

#### `ApplyVoucherRequest.java`
```java
package com.rhotels.crm.dto;

import com.fasterxml.jackson.annotation.JsonPropertyDescription;

public record ApplyVoucherRequest(
    @JsonPropertyDescription("Mã đơn đặt phòng / mã hóa đơn cần áp dụng mã giảm giá (ví dụ: HD999)")
    String invoiceId,

    @JsonPropertyDescription("Mã giảm giá được áp dụng (ví dụ: VIP20)")
    String voucherCode
) {}
```

#### `ApplyVoucherResponse.java`
```java
package com.rhotels.crm.dto;

public record ApplyVoucherResponse(
    boolean isSuccess,
    String invoiceId,
    String voucherCode,
    double originalAmount,
    double discountAmount,
    double finalAmount,
    String message
) {
    public static ApplyVoucherResponse error(String message) {
        return new ApplyVoucherResponse(false, null, null, 0.0, 0.0, 0.0, message);
    }

    public static ApplyVoucherResponse success(String invoiceId, String voucherCode, double originalAmount, double discountAmount, double finalAmount, String message) {
        return new ApplyVoucherResponse(true, invoiceId, voucherCode, originalAmount, discountAmount, finalAmount, message);
    }
}
```

#### `CrmChatResponse.java`
```java
package com.rhotels.crm.dto;

public record CrmChatResponse(
    String conversationId,
    String replyMessage
) {}
```

---

### 3.2. Service tích hợp Tools (`CrmVoucherService.java`)

```java
package com.rhotels.crm.service;

import com.rhotels.crm.dto.*;
import org.springframework.ai.tool.annotation.Tool;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class CrmVoucherService {

    @Tool(description = "Truy vấn danh sách các mã giảm giá (voucher) còn hạn của khách hàng dựa trên customerId (Mã khách hàng CRM, ví dụ: KH888). " +
                        "Chỉ gọi công cụ này khi đã xác định được mã khách hàng.")
    public CustomerVouchersResponse getCustomerVouchers(CustomerVouchersRequest request) {
        if (request == null) {
            return CustomerVouchersResponse.error("Yêu cầu không hợp lệ: Request payload bị null.");
        }
        if (request.customerId() == null || request.customerId().isBlank()) {
            return CustomerVouchersResponse.error("Mã khách hàng (customerId) không được để trống. Vui lòng hỏi lại khách hàng.");
        }

        String customerIdStr = request.customerId().trim().toUpperCase();

        if ("KH888".equals(customerIdStr)) {
            List<VoucherDTO> vouchers = List.of(
                new VoucherDTO("VIP20", "Giảm 20% cho khách hàng VIP (Giảm tối đa 500.000 VNĐ)", 20.0, 500000.0, false),
                new VoucherDTO("WELCOME10", "Giảm 10% chào mừng thành viên mới (Giảm tối đa 200.000 VNĐ)", 10.0, 200000.0, false)
            );
            return CustomerVouchersResponse.success(customerIdStr, vouchers, 
                "Tìm thấy 2 mã giảm giá khả dụng cho khách hàng " + customerIdStr);
        } else if ("KH777".equals(customerIdStr)) {
            List<VoucherDTO> vouchers = List.of(
                new VoucherDTO("WELCOME10", "Giảm 10% chào mừng thành viên mới", 10.0, 200000.0, false)
            );
            return CustomerVouchersResponse.success(customerIdStr, vouchers, 
                "Tìm thấy 1 mã giảm giá khả dụng cho khách hàng " + customerIdStr);
        } else {
            return CustomerVouchersResponse.error("Không tìm thấy hồ sơ khách hàng mã '" + customerIdStr + "' trong hệ thống CRM.");
        }
    }

    @Tool(description = "Áp dụng một mã giảm giá (voucherCode) cụ thể vào hóa đơn/đơn đặt phòng (invoiceId). " +
                        "Công cụ này sẽ cập nhật trực tiếp số tiền phải thanh toán vào database hệ thống.")
    public ApplyVoucherResponse applyVoucherToInvoice(ApplyVoucherRequest request) {
        if (request == null) {
            return ApplyVoucherResponse.error("Yêu cầu không hợp lệ: Request payload bị null.");
        }
        if (request.invoiceId() == null || request.invoiceId().isBlank()) {
            return ApplyVoucherResponse.error("Mã hóa đơn (invoiceId) không được để trống.");
        }
        if (request.voucherCode() == null || request.voucherCode().isBlank()) {
            return ApplyVoucherResponse.error("Mã giảm giá (voucherCode) không được để trống.");
        }

        String invoiceIdStr = request.invoiceId().trim().toUpperCase();
        String voucherCodeStr = request.voucherCode().trim().toUpperCase();

        if ("HD888".equals(invoiceIdStr)) {
            return ApplyVoucherResponse.error("Lỗi nghiệp vụ: Hóa đơn " + invoiceIdStr + " đã được thanh toán từ trước. Không thể áp dụng mã giảm giá.");
        }

        if (!"HD999".equals(invoiceIdStr)) {
            return ApplyVoucherResponse.error("Không tìm thấy đơn đặt phòng / hóa đơn mã '" + invoiceIdStr + "' trong hệ thống.");
        }

        double originalAmount = 4000000.0;
        double discountPercentage = 0.0;
        double maxDiscount = 0.0;

        if ("VIP20".equals(voucherCodeStr)) {
            discountPercentage = 20.0;
            maxDiscount = 500000.0;
        } else if ("WELCOME10".equals(voucherCodeStr)) {
            discountPercentage = 10.0;
            maxDiscount = 200000.0;
        } else {
            return ApplyVoucherResponse.error("Mã giảm giá '" + voucherCodeStr + "' không hợp lệ hoặc đã hết hạn sử dụng.");
        }

        double rawDiscount = originalAmount * (discountPercentage / 100.0);
        double actualDiscount = Math.min(rawDiscount, maxDiscount);
        double finalAmount = originalAmount - actualDiscount;

        return ApplyVoucherResponse.success(
            invoiceIdStr, 
            voucherCodeStr, 
            originalAmount, 
            actualDiscount, 
            finalAmount, 
            String.format("Áp dụng mã %s thành công cho hóa đơn %s. Đã giảm %,.0f VNĐ. Số tiền cần thanh toán còn lại: %,.0f VNĐ.",
                    voucherCodeStr, invoiceIdStr, actualDiscount, finalAmount)
        );
    }
}
```

---

### 3.3. REST Controller (`CrmAgentController.java`)

```java
package com.rhotels.crm.controller;

import com.rhotels.crm.dto.CrmChatResponse;
import com.rhotels.crm.service.CrmVoucherService;
import org.springframework.ai.chat.client.ChatClient;
import org.springframework.ai.chat.client.advisor.MessageChatMemoryAdvisor;
import org.springframework.ai.chat.memory.ChatMemory;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.UUID;

@RestController
@RequestMapping("/api/crm")
public class CrmAgentController {

    private final ChatClient chatClient;

    public CrmAgentController(ChatClient.Builder builder, CrmVoucherService crmVoucherService, ChatMemory chatMemory) {
        this.chatClient = builder
                .defaultSystem("""
                    Bạn là Trợ lý Chăm sóc Khách hàng (CRM Support Agent) thông minh của hệ thống R-Hotels.
                    
                    NHIỆM VỤ CỦA BẠN:
                    1. Hỗ trợ khách hàng tra cứu mã giảm giá (voucher) và áp dụng voucher ưu đãi nhất vào đơn đặt phòng/hóa đơn.
                    
                    QUY TRÌNH XỬ LÝ (IMPORTANT REACT WORKFLOW):
                    - ĐỌC LỊCH SỬ CHAT (ChatMemory): Trước tiên hãy kiểm tra lịch sử trò chuyện xem thông tin danh tính khách hàng (như Mã khách hàng customerId, ví dụ: KH888) đã có hay chưa.
                    - NẾU CHƯA CÓ danh tính khách hàng: Lịch sự yêu cầu khách hàng cung cấp Mã khách hàng (customerId) hoặc số điện thoại đăng ký trước khi tiếp tục.
                    - NẾU ĐÃ CÓ customerId:
                      + Bước 1: Gọi Tool 'getCustomerVouchers' để lấy danh sách tất cả các voucher còn hiệu lực của khách hàng đó.
                      + Bước 2: Phân tích danh sách trả về, so sánh các mức chiết khấu và tự động chọn ra mã giảm giá mang lại lợi ích tốt nhất cho khách hàng.
                      + Bước 3: Tự động gọi tiếp Tool 'applyVoucherToInvoice' để áp dụng mã giảm giá tốt nhất đó vào đơn đặt phòng/hóa đơn (invoiceId).
                      + Bước 4: Tổng hợp kết quả phản hồi cuối cùng và trả lời bằng ngôn ngữ tự nhiên thân thiện.
                    
                    RÀNG BUỘC PHÒNG THỦ:
                    - Nếu bất kỳ Tool nào trả về 'isSuccess = false', hãy đọc lý do lỗi trong trường 'message' và giải thích lịch sự lý do cho khách hàng. KHÔNG NỔ EXCEPTION.
                    """)
                .defaultAdvisors(new MessageChatMemoryAdvisor(chatMemory))
                .defaultTools(crmVoucherService)
                .build();
    }

    @GetMapping("/chat")
    public ResponseEntity<CrmChatResponse> chat(
            @RequestParam String message,
            @RequestParam(required = false) String conversationId) {

        String effectiveConversationId = (conversationId != null && !conversationId.isBlank())
                ? conversationId.trim()
                : UUID.randomUUID().toString();

        String replyMessage = this.chatClient.prompt()
                .user(message)
                .advisors(advisorSpec -> advisorSpec
                        .param("chat_memory_conversation_id", effectiveConversationId)
                        .param("chat_memory_retrieve_size", 20)
                )
                .call()
                .content();

        return ResponseEntity.ok(new CrmChatResponse(effectiveConversationId, replyMessage));
    }
}
```

---

## 4. PHÂN TÍCH KỸ THUẬT CHUYÊN SÂU VỀ LUỒNG GỌI TOOL LIÊN TIẾP (TOOL CHAINING)

### 4.1. Bản chất cơ chế ReAct Loop trong Spring AI Function Calling
Luồng gọi công cụ liên tiếp (**Multi-step Tool Chaining**) không dựa trên mã lệnh hardcode tĩnh của Java mà là một chu trình suy luận tự động của LLM kết hợp với Spring AI Engine theo mô hình **ReAct (Reasoning + Acting)**:

```
[Vòng 1: User Request] ──► [LLM Suy luận: Cần lấy danh sách Vouchers]
                                         │
                                         ▼ (Gọi Tool 1)
                           getCustomerVouchers("KH888")
                                         │
[Vòng 2: Tool 1 Output] ◄────────────────┘ (Trả về JSON list vouchers)
         │
         ▼
[LLM Suy luận & So sánh: VIP20 là tốt nhất (giảm 500k vs 200k)]
         │
         ▼ (Tự động gọi Tool 2 với thông tin từ Tool 1 + Request)
applyVoucherToInvoice("HD999", "VIP20")
         │
[Vòng 3: Tool 2 Output] ◄────────────────┘ (Trả về JSON thành công)
         │
         ▼
[LLM Tổng hợp câu trả lời tự nhiên cuối cùng gửi cho User]
```

---

### 4.2. Chi tiết cách Spring AI truyền dữ liệu giữa Tool 1 và Tool 2

1. **Vòng lặp 1 (Tool 1 Invocation):**
   * LLM phân tích câu hỏi người dùng và lịch sử chat -> Phát hiện `customerId = "KH888"`.
   * LLM tạo `ToolCallRequest` với hàm `getCustomerVouchers` và tham số `{"customerId": "KH888"}`.
   * Spring AI nhận yêu cầu, dùng Reflection gọi phương thức `getCustomerVouchers(...)` trong `CrmVoucherService`.
   * Java trả về đối tượng `CustomerVouchersResponse`. Spring AI serialize đối tượng này thành chuỗi JSON:
     ```json
     {
       "isSuccess": true,
       "customerId": "KH888",
       "vouchers": [
         {"code": "VIP20", "discountPercentage": 20.0, "maxDiscountAmount": 500000.0},
         {"code": "WELCOME10", "discountPercentage": 10.0, "maxDiscountAmount": 200000.0}
       ],
       "message": "Tìm thấy 2 mã giảm giá..."
     }
     ```
2. **Nạp dữ liệu vào Ngữ cảnh Context Window (Observe Step):**
   * Chuỗi JSON kết quả của Tool 1 được Spring AI chèn ngược trở lại vào danh sách tin nhắn ngữ cảnh hội thoại dưới dạng tin nhắn loại `ToolResponseMessage` (hay `FunctionResponseMessage`).
3. **Vòng lặp 2 (Tool 2 Invocation - Automatic Parameter Extraction):**
   * Mô hình LLM đọc toàn bộ chuỗi JSON trả về từ Tool 1.
   * LLM thực hiện phép so sánh suy luận logic (Reasoning): `20% (max 500k) > 10% (max 200k)` -> Mã tốt nhất là `"VIP20"`.
   * Trích xuất `invoiceId = "HD999"` từ phát ngôn ban đầu của người dùng và `voucherCode = "VIP20"` từ kết quả Tool 1 vừa chèn vào ngữ cảnh.
   * LLM phát ra `ToolCallRequest` thứ hai dành cho Tool `applyVoucherToInvoice` với tham số đóng gói:
     ```json
     {
       "invoiceId": "HD999",
       "voucherCode": "VIP20"
     }
     ```
   * Spring AI tiếp tục thực thi Tool 2 và trả lại kết quả `ApplyVoucherResponse` dưới dạng JSON cho LLM.
4. **Vòng lặp 3 (Final Response Synthesis):**
   * Sau khi nhận kết quả từ Tool 2 (áp dụng thành công, giảm 500.000 VNĐ), LLM nhận thấy không cần gọi thêm Tool nào nữa.
   * LLM tổng hợp toàn bộ lịch sử thành câu phản hồi tự nhiên duy nhất cho người dùng:
     > *"Dạ chào anh/chị! Em đã kiểm tra các ưu đãi của mã khách hàng KH888 và áp dụng mã giảm giá tốt nhất là **VIP20** (Giảm 20%, tối đa 500.000 VNĐ) vào đơn đặt phòng **HD999** của anh/chị rồi ạ. Hóa đơn gốc 4.000.000 VNĐ sau khi giảm còn lại **3.500.000 VNĐ** cần thanh toán ạ!"*
